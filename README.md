# cgrossde/homebrew

Homebrew tap for public cgrossde CLI tools.

## Install

```sh
brew tap cgrossde/homebrew
```

Then install individual tools:

```sh
brew install jiracli   # Easy, agent-friendly & scriptable Jira access
brew install slackcli  # Slack CLI optimized for usage by LLMs or in scripts
```
